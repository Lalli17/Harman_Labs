import { add, multiply } from './math.js';

let a=10;
let b=20;

console.log(`The sum of ${a} and ${b} is ${add(a, b)}`); // Output: The sum of 10 and 20 is 30
console.log(`The product of ${a} and ${b} is ${multiply(a, b)}`); // Output: The product of 10 and 20 is 200